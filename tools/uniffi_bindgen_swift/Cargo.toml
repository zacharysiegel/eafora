[package]
name = "uniffi_bindgen_swift"
version = "0.0.0"
edition.workspace = true
publish = false

[[bin]]
name = "uniffi-bindgen-swift"
path = "src/main.rs"

[dependencies]
# cli carries the Swift bindgen entry point.
uniffi = { workspace = true, features = ["cli"] }
