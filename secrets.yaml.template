# Encrypted secret store. Each entry is a base64-encoded ChaCha20-Poly1305
# ciphertext produced by `secr encrypt --key <MASTER_SECRET> --name <name> <plaintext>`.
# `secrets.yaml` itself is gitignored; this template documents the expected keys.

r2_access_key_id: REPLACE_WITH_SECR_ENCRYPT_OUTPUT
r2_secret_access_key: REPLACE_WITH_SECR_ENCRYPT_OUTPUT
