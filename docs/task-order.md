# Task order

Committed near-term work sequence. Order is load-bearing (later tasks depend on earlier ones). Detail for each item lives in `docs/backlog.md` (parked items, with triggers) or in the architecture doc that introduces the work.

When a task is picked up, leave it here as **In progress**; delete on the same PR that lands it on master.

## Sequence

1. **`docs-architecture-client-web`** — per-platform deep-dive doc; flagged in `docs/architecture/client.md`. **In progress** (branch: `docs-architecture-client-web`).
2. **`docs-architecture-client-ios`** — per-platform deep-dive doc; flagged in `docs/architecture/client.md`. Web + iOS architecture docs advance in parallel.
3. **`/speckit.specify` for the web client** — first client-implementation feature.
4. **`/speckit.specify` for the iOS client** — second client-implementation feature.
