// The SSR-build binary cargo-leptos compiles with the `ssr` feature. Static-site generation for the
// /region and /about routes is a follow-up feature and no static routes are declared yet; wiring the
// generator (and the dev-server integration) is the open SSR-integration decision, so for now this is
// a placeholder that compiles under `ssr`.
fn main() {}
