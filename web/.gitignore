# The downsampled embedded bundle is synced in by scripts/build/sync-embedded-bundle.sh and rebuilt each CI
# run (see specs/003-web-client/plan.md, FR-004); it is not checked in.
/static/embedded_artifacts/
/static/repository/

# wrangler's local state and temporary bundles, written on every deploy.
/.wrangler/
