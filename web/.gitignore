# The downsampled embedded bundle is synced in by scripts/sync-embedded-bundle.sh and rebuilt each CI
# run (see specs/003-web-client/plan.md, FR-004); it is not checked in.
/static/embedded_artifacts/
/static/repository/
