# Same format as .gitignore; wrangler skips matching files when uploading.

# Local publish output the dev server serves at /repository.
/repository/

# wasm-bindgen type declarations, which cargo-leptos gives no way to suppress.
/pkg/*.ts

.DS_Store
