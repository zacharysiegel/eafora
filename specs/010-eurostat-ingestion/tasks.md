# Tasks: Eurostat ingestion, mean age at childbirth and at first birth

**Feature**: 010-eurostat-ingestion | **Branch**: `010-eurostat-ingestion` | **Date**: 2026-09-01

**Input**: [spec.md](spec.md), [plan.md](plan.md)

Organized by the plan's phases. Phase A and Phase B each land as their own PR on a linear stack, A first, because the enum variants A adds are what B compiles against. Phases C and D are sketches in the plan and carry no task breakdown; each needs a probe before it can get one.

Read `docs/conventions/types.md` before naming any new type, and take the names the plan's §Module layout pins rather than inventing parallel ones.

Every decision the draft left open is now settled in spec.md §Decisions taken. Take the values from there rather than re-deriving them; two facts remain open in §Open questions and gate Phases C and D only.

---

## Phase A: canonical vocabulary and presentation

- [ ] T001 `./scripts/git/branch-init.sh eurostat-canonical-vocabulary` from a clean `master`.

### Decisions to close before writing anything

- [ ] T002 Take the settled names from spec.md §Decisions taken: codes `mean_age_at_childbirth` and `mean_age_at_first_birth`, variants `MeanAgeAtChildbirth` and `MeanAgeAtFirstBirth`, one `data_source` row coded `eurostat`, licence `Commission Decision 2011/833/EU` pointing at Eurostat's copyright-notice page. Correct the `data_source.code` column comment, whose example names a dataset rather than a publisher.

### The canonical enums

- [ ] T003 Add `Estimated` to `DataStatus` in `shared/src/canonical/canonical_model.rs`, with arms in `as_str()` (:163) and `TryFrom<&str>` (:175). Both directions are mandatory: the value is written as text by `ingestion/src/ingest/ingest_db.rs:101` and parsed back at `ingestion/src/canonical/canonical_entity.rs`, `ingestion/src/artifact/artifact_model.rs:62`, and both target-gated readers in `shared/src/sqlite/shard_db.rs`. `impl_code_serde!` at :191 needs no change. Run `cargo test -p shared`. Expected: PASS, including the existing case that asserts an unknown status is rejected.
- [ ] T004 Add the Eurostat variant to `DataSourceKind` (`shared/src/canonical/canonical_model.rs:124`) with its `code()` (:130) and `TryFrom<&str>` (:138) arms, using the code T002 settled. Run `cargo check -p shared`. Expected: clean. `cargo check -p web --lib` will now fail on the exhaustive `source_label` match; that is T010's work.
- [ ] T005 Add the two `StatisticKind` variants (:76) with arms in `temporal_basis()` (:91), `code()` (:98) and `TryFrom<&str>` (:106), and extend the temporal-basis test at :325. Both report `TemporalBasis::Period`. Declare them after `Tfr` and `Ccf`: the derived `Ord` at :75 fixes the manifest's statistic key order and the picker's option order, and `ingestion/tests/artifact_integration.rs:80-88` asserts the first shard of a build is `tfr`. Expected: `cargo test -p shared` FAILS on the non-exhaustive `transform_for` match at `shared/src/map/color.rs:100`, which T009 closes. The workspace does not compile again until T011, so run no wrapper script that compiles it before then.

### The migration

- [ ] T006 Write `ingestion/db/migrations/<timestamp>_seed_eurostat_and_mean_age_statistics.sql`. Up: the `data_source` row at `preference_rank` 40 with `license_class='attribution'`; the two `statistic` rows with `units='years'` and `name_abbreviated_en`; the widened `statistic_value.data_status` catalog comment; the corrected `data_source_publication.revision_label` catalog comment. The statistic column list is `(code, name_en, name_abbreviated_en, units)` and nothing else: `description` was dropped by `20260825120000_drop_statistic_description.sql`, so `20260819140100_seed_hfd_and_completed_cohort_fertility.sql:8` is not a template for it. Down: delete `statistic_value` by statistic and by source separately, then `data_source_publication` by source, then the two `statistic` rows and the `data_source` row. Do not copy the HFD seed's `source_choice` deletes; that table is dropped by `20260822140000_drop_source_choice.sql` and does not exist at this migration's point.
- [ ] T007 Apply it, either before T003 or after T011, never between them: the wrapper ends in `cargo sqlx prepare --workspace -- --all-targets`, which compiles the workspace, and T003 through T010 leave it non-exhaustive. The migration depends on nothing the enums add, so either order works. `./scripts/db/dbmate.sh up`, then `./scripts/db/setup-test-db.sh`. Never a bare `dbmate`; the wrapper carries the URL, both paths, and the `cargo sqlx prepare --workspace -- --all-targets` that follows. Verify the down migration too (`./scripts/db/dbmate.sh down` then `up`), since a broken down migration is only found by running it. Commit the regenerated `ingestion/db/schema.sql` and the `.sqlx` cache. Expected: the two statistic rows and the source row present, and `select code, preference_rank from data_source order by preference_rank asc;` reading 40, 50, 100.

### Presentation

- [ ] T008 Apply the two settled presentation decisions: the mean-age transform saturates at its highest value, not its lowest, and the chart's reference stays independent of the colour transform while the duplicated replacement-rate constant moves into one place in `shared` that both sites read. Also settle the per-statistic decimal places here, one for an age and two for a rate, since T010's label arms carry it.
- [ ] T009 Add the two arms to `transform_for` in `shared/src/map/color.rs:100`, selecting the range-normalized transform whose `inflection()` is already `None` (:92). Reword the polarity claim in `CHOROPLETH_SCALE`'s doc comment at :56, which justifies red-at-the-lowest-value as the fertility direction and stops being true of every statistic the scale serves. Add a host test asserting the existing curve saturates for age-range inputs (positions 0.9941 at 24 and 0.9957 at 32, so a span of under 0.2 % of the ramp) and that the new transform spreads the same span across it; that arithmetic is the whole reason for the change and should be pinned rather than asserted in prose. Run `cargo test -p shared`. Expected: PASS.
- [ ] T010 Add the compiler-forced arms: `statistic_label`, `statistic_unit`, `statistic_description` and `reference_caption` in `web/src/map/labels.rs:8-44` (the caption returns `None` for both new statistics), `source_label` at `web/src/map/detail_panel.rs:1004`, and `status_text` and `status_label` at `:981` and `:993`. Also `reference_value` at `:484`, returning `None` for both new statistics. Run `cargo check -p web --lib`. Expected: fails only on the missing locale keys, which T011 adds.
- [ ] T011 Add the locale strings to `web/locales/en.json`: label, unit and description per new statistic in the `statistic` block (:35-43), the source label in the `source` block (:52-55), and the estimated sentence in `detail.status` (:13-19). Units are lowercase fragments, matching "births per woman" and "children per woman", because they render as a standalone line under a value (`detail_panel.rs:204`, `:266`) and as the rotated chart axis title (`:450`, `:658`). The status string is a full sentence ending in a period, matching its five neighbours. `web/build.rs` generates the i18n module from this file, so the keys are compile-checked. Run `cargo test -p web --lib`. Expected: PASS.
- [ ] T012 Make the chart's absent reference actually render nothing. `ChartGeometry` (`web/src/map/detail_panel.rs:407`) carries whether a reference exists, both constructors set it, and the dashed line and its value label at `:660-674` are gated by a visibility class the way the cursor is at `:682-689`, copying the pattern from `.region-dock-chart-cursor` at `web/style/components/_detail-panel.scss:256-262` and placing the new rule beside `.region-dock-chart-reference` at `:250-254`. Do not omit the elements: the comments at `:774-777` and `:803-806` record that Safari logs an invalid empty attribute value for every attribute Leptos removes when it tears an SVG element down, which is why the cursor hides rather than unmounts. Without this the `None` branch falls back to the plot's baseline at `:448` and draws a dashed line over the solid axis with an empty label.
- [ ] T013 Add the counterpart to the existing scale test at `web/src/map/detail_panel.rs:1246-1254`: `ChartScale::from_series` over an age-range series with `None` for the reference spans the plot rather than its top fifth. Assert the absent reference separately, on `ChartGeometry`'s new field rather than on `ChartScale`, which has no reference field; `chart_scale_draws_a_flat_series_inside_the_plot` already passes `None`, so the new value is the age range rather than the `None` itself. The existing test asserts the reference is held inside the range, which is the behaviour that must not happen here; the new case pins the branch that only exists once `reference_value` can return `None`. Run `cargo test -p web --lib`. Expected: PASS.

### Documentation

- [ ] T014 Correct `docs/architecture/ingestion.md` §Current preference ranking, whose table says HFD 10 / Eurostat 30 / World Bank 90 against shipped values of 50 / 40 / 100, and replace that table's "~25 countries" with "approx. 25 countries", checking its other rows for tildes. Remove §Merge rule step 4, the freshness override: nothing implements it, rank alone decides, and rank 40 was chosen against that behaviour. Mark `docs/task-order.md` item 1 as **In progress** with the branch name, per the standing convention, and describe its phases beneath it as item 2 already does for the iOS client. Do not delete it when Phase B merges: the item's substance is the subnational work, which Phases C and D do not deliver, so Phase B rewrites it down to what remains unlanded.

### Phase A verification

- [ ] T015 Host sweep: `cargo test -p shared`, `cargo test -p ingestion`, `cargo test -p web --lib`, `cargo check -p web --lib --no-default-features --features hydrate --target wasm32-unknown-unknown`, and `cargo check -p web --no-default-features --features ssr`, the repository's second documented web build, which expands the i18n macros differently. Expected: clean throughout. Not `cargo test -p shared --features render`: no render-gated code is touched, since the colour transform is computed on the CPU and uploaded as a vertex attribute rather than mirrored in WGSL.
- [ ] T016 Confirm Phase A publishes nothing new. `cargo run --release -p ingestion -- build`, then read the log: the two new statistics log as having no candidate values and are skipped (`ingestion/src/artifact/artifact.rs:132-138`), no new shard is written, and the manifest's key space is unchanged. This is what makes the phase reviewable without the adapter, so it is worth confirming rather than assuming.
- [ ] T016a Self-review pass over Phase A before opening its PR, against the root `CLAUDE.md` and `docs/conventions/`. The migration is the feature's only SQL and is where this bites: lowercase keywords, explicit `insert` column lists, inline and unnamed constraints, `if exists` on the down migration's deletes, the trailing semicolon on its own line, and single-quoted strings with doubled apostrophes rather than dollar-quoting in both catalog comments. Also check the Rust for em dashes in comments, `let` bindings carrying explicit types, and doc comments that restate what the code says.
- [ ] T016b `gh pr create --base master --assignee zacharysiegel` with the body from plan.md §PR description, Phase A. No em dashes in it.

---

## Phase B: the Eurostat adapter

- [ ] T017 `./scripts/git/branch-init.sh eurostat-adapter`, stacked on the Phase A branch, not on `master`.

### Shared groundwork

- [ ] T018 Move `should_skip_run` (`ingestion/src/hfd/hfd_adapter.rs:97-110`) and its four unit tests (`:248-272`) to a new `ingestion/src/adapter/adapter.rs`, re-exported by `ingestion/src/adapter/mod.rs` with `pub mod adapter;` and `pub use adapter::*;`. Pure motion; change no behaviour. The private test builders move with it. Run `cargo test -p ingestion should_skip_run`. Expected: PASS, four tests, unchanged assertions. Editing an assertion means the move changed something.
- [ ] T019 Add `find_country_by_iso2` to `ingestion/src/canonical/canonical_db.rs`, copied from `find_country_by_iso3` (:10-26) with the predicate changed. `country.iso2` is not-null and unique (`country_iso2_key` in `ingestion/db/schema.sql:362`), so `fetch_optional` can match at most one row by constraint rather than by luck. Run `./scripts/db/dbmate.sh up` to regenerate the sqlx cache (the schema is already applied, so this is a no-op migration run whose purpose is the `cargo sqlx prepare` that follows), then `cargo check -p ingestion --all-targets`. Expected: clean. `--lib` alone would not surface a cache miss in `tests/`.

### Samples, captured not written

- [ ] T020 Write `ingestion/tests/eurostat_live.rs`: one `#[tokio::test] #[ignore = "requires network access"]` that fetches, prints the response size and the top-level `updated`, and writes the body to a named path under `/tmp` with the destination printed. Model on `ingestion/tests/hfd_live.rs:1-28`, whose doc comment states the same intent and is why the HFD samples are trustworthy. Invocation: `cargo test -p ingestion --test eurostat_live -- --ignored --nocapture`.
- [ ] T021 Capture and commit three samples under `ingestion/samples/eurostat/`, each an unedited live response: the phase-one extraction (three indicators, country level; 9,555 addressable cells, 5,001 observations and 136 flag entries as captured on 2026-09-01, distributed `b` 64, `e` 38, `ep` 18, `p` 15, `be` 1, with `f`, `i`, `m`, `d`, `n` and `u` absent from the dataset; record what the capture actually yields rather than these, the flagged-valueless cell, both alias codes, and the excluded metropolitan-France code); a small dense multi-dimensional slice (two indicators, two countries, two years, eight cells, four flagged) whose every cell can be asserted by hand and whose indicator index proves positions are not request order; and a single unflagged cell whose response omits the flag map entirely. Do not hand-trim a JSON-stat document: deleting an observation without fixing the sizes and index maps yields a document that is not a valid response, and a parser test against one proves nothing. The third sample is not optional; without it, dropping the default on the flag field passes every other test and fails in production on the first slice with no flagged cell.

### Parsing, tests first

- [ ] T022 Write the failing parse tests in a `#[cfg(test)] mod tests` at the bottom of `ingestion/src/eurostat/eurostat_client.rs`, over the three samples pulled in with `include_str!` as `ingestion/src/hfd/hfd_client.rs:308-309` does. Cases: the publication's revision label is the raw `updated` string and its instant is that string parsed (the offset carries no colon, so the RFC 3339 parser fails and an explicit format is required); the parsed observation count equals the value map's length and not the flag map's; a named cell in each of the three indicators carries the expected value; the flagged-valueless cell produces no observation; the dense sample's eight cells each decode to the right indicator, country and year; the response with no flag map parses; and one rejection case each for a missing dimension, a category index that is not a bijection onto its declared size, and a body that is not the expected document. Run `cargo test -p ingestion eurostat`. Expected: does not compile, the module does not exist.
- [ ] T023 Add `ingestion/src/eurostat/mod.rs` (declarations and re-exports only) and `ingestion/src/eurostat/eurostat_model.rs` (the wire response types and the two parsed types). The observation and flag maps deserialize into integer-keyed ordered maps directly; serde parses a stringified-integer object key into an integer map key. The flag map takes `#[serde(default)]`. Do not add `deny_unknown_fields`: the response carries five top-level fields this feature does not model. Keep the flag as a `String` on the parsed observation, because the client may not name `DataStatus`. Register the module in `ingestion/src/lib.rs`, keeping the list alphabetical. Run `cargo check -p ingestion`. Expected: clean.
- [ ] T024 Implement the parse in `eurostat_client.rs`: the publication, the per-dimension positional inversion of each category index map with its bijection check, the stride decomposition, and iteration over the value map with a lookup into the flag map. Constants (the endpoint base, the dataset, the three indicator codes) at the top of the file, before the types. Errors as `AppError::from(format!("prose; [key=value ...]"))` per `docs/conventions/logging.md`, following `hfd_client.rs:73` rather than the older World Bank prose. Run `cargo test -p ingestion eurostat`. Expected: PASS, every case from T022.
- [ ] T025 Implement `fetch_upstream`, raising both failures as `AppError::from(format!("prose; [key=value ...]"))` per `docs/conventions/logging.md`, following `hfd_client.rs:216-220` rather than the older prose shape at `world_bank_wdi_client.rs:54`: one request through `crate::http::HTTP_CLIENT` with the dataset, `format=JSON`, `lang=EN`, the country-level geo filter and the three indicator filters. No `AdapterOptions` parameter; Eurostat cannot narrow the request by revision, and the World Bank client's unused options parameter at `world_bank_wdi_client.rs:14` is the shape to avoid. A non-success status fails with the status and the URL. A body that is not the expected document fails naming the dataset and the cell limit, following the guard at `hfd_client.rs:214-222` that catches a login page served under status 200. Run `cargo test -p ingestion --test eurostat_live -- --ignored --nocapture`. Expected: a response whose `updated` and size match the committed sample's.

### The flag mapping, tests first

- [ ] T026 Write the failing precedence tests in `ingestion/src/eurostat/eurostat_adapter.rs`'s test module: each of the nine atomic characters; the three concatenations that occur most in the real data plus one longer one; an absent flag; and a flag naming only unmodelled qualifiers. Assert the owner's ordering directly (forecast beats imputed beats estimated beats provisional), so a later reordering fails here rather than in an integration test. The forecast character does not occur anywhere in this dataset, so its case is a synthetic input and the only coverage that arm will ever have. Run `cargo test -p ingestion eurostat`. Expected: does not compile.
- [ ] T027 Implement the mapping as one ordered const table of character to status, scanned for membership, in `eurostat_adapter.rs`, `pub` for the tests. Every atomic code is a single character and every concatenation an ordered run of them, so membership is sound and no tokenizer is needed. Do not read the response's per-response status label block: it holds only the codes that response used and is a display aid. Run `cargo test -p ingestion eurostat`. Expected: PASS.

### Normalization

- [ ] T028 Add the geo alias table and the exclusion table to `eurostat_adapter.rs`, each one const array of paired code and target following `ingestion/src/hfd/hfd_adapter.rs:20-29`, with a one-line comment stating the external fact each encodes (Eurostat publishes Greece and the United Kingdom under codes that are not their ISO alpha-2; the metropolitan-France code is a subset of France and survives the country-level filter). Add the resolved-or-warned outcome enum, as `:31-34` has. An excluded code yields no row and no warning; anything else unresolvable warns. Verified: the country-level filter narrows the geo dimension from 58 positions to 49, dropping the aggregate codes and the whole-Germany code, and Kosovo's code already matches the seeded value, so neither needs an entry.
- [ ] T029 Implement `resolve_region`, `normalize_row` and `normalize` in `eurostat_adapter.rs`, mirroring `hfd_adapter.rs:190`, `:213` and `:114` including the `statistic_kind` parameter and the "(run dbmate up)" error when the statistic is missing. The caller filters the parsed observations by indicator code before each call, so `normalize` stays indicator-agnostic. Period via the existing single-year constructor at `shared/src/canonical/canonical_model.rs:296`. Status from T027's mapping rather than the hardcoded `DataStatus::Final` both existing adapters use. Run `cargo check -p ingestion --all-targets`. Expected: clean.
- [ ] T030 Implement `fetch_and_store`: begin the transaction, resolve the source row through `canonical_db::find_data_source_by_kind`, read the latest publication, fetch, parse, `should_skip_run` with an early empty report, normalize three times, concatenate, `ingest::record_statistic_values`, attach the warnings to the report, commit. One transaction per run, `&mut *transaction` to every helper, `&PgPool` nowhere below this function. Run `cargo check -p ingestion --all-targets`. Expected: clean.

### Integration

- [ ] T031 Write `ingestion/tests/eurostat_integration.rs` against `eafora_test`, following `ingestion/tests/hfd_integration.rs` including its module doc comment about the rollback discipline. Each test: a fresh pool from `helpers::test_db::test_pool()`, a transaction, the assertions, `transaction.rollback()` last. Source-specific helpers (parse, normalize, record over the sample) stay private in this file; only `helpers::canonical` and `helpers::adapter` are shared. Cases: a bare alpha-2 code resolves; both alias codes resolve to their countries; the excluded code yields no row and no warning; an unresolvable code yields one warning and no row; each indicator lands on its own statistic, by three separate statistic lookups; a flagged observation's status reaches `statistic_value.data_status`; and the added, unchanged-second-run, and superseded-revision triple with the raw count assertion that proves both rows survive. Run `./scripts/db/setup-test-db.sh` then `cargo test -p ingestion --test eurostat_integration`. Expected: PASS. Not under `SQLX_OFFLINE`.

### Wiring

- [ ] T032 Wire the CLI in `ingestion/src/main.rs`: the import beside `:20-24`, the entry in `REGISTERED_SOURCES` at `:28`, the dispatch arm in `run_source` at `:156`, and the source argument's help text at `:62`, which names the registered codes and is not compiler-checked. The match is exhaustive so the compiler names it; the slice is not, and omitting the entry silently drops Eurostat from the all-sources run. Run `cargo run -p ingestion -- ingest source --help`. Expected: the help text names all three codes.

### The first real run

- [ ] T033 Run the adapter against live Eurostat: `cargo run --release -p ingestion -- ingest source <code>`. Reconcile the report's counts against the response: the observation count against the value map's length, the warning count against the geo codes that resolve to nothing. Run it a second time and confirm it writes nothing. Record both figures in §Deviations, because they are the numbers the spec's success criteria are read against.
- [ ] T034 Bump `MANIFEST_SCHEMA_VERSION` (`shared/src/artifact/manifest.rs:13`) and leave the shard's `SCHEMA_VERSION` alone, per spec.md §Decisions taken item 4. Verify the backtracking path by hand as specs/008 describes: point `latest/manifest.json` at the new version and confirm an older client requests the previous schema's pointer key.
- [ ] T035 Extend the downsample filter at `ingestion/src/artifact/artifact.rs:185-215` to keep one reference year per statistic rather than only the World Bank's United States anchor, so the picker offers the same three statistics before and after the live swap. Its warning at `:200-203` should name the statistic it found nothing for. Add the per-statistic coverage note to the picker's locale strings.
- [ ] T036 End to end locally, in order: `cargo run --release -p ingestion -- build`, `cargo run --release -p ingestion -- publish local`, `./scripts/build/sync-embedded-bundle.sh ./web/static/embedded_artifacts`, `./scripts/build/verify-site-tree.sh`, `./scripts/build/measure-site-budget.sh`. The embedded re-sync is mandatory if T034 bumped the manifest version: the embedded manifest is parsed by the same reader, so a bump against a stale tree leaves first paint with no bundle at all. Record the budget figures in §Deviations.
- [ ] T037 Look at it. `cargo leptos watch`, then for each new statistic: the covered countries paint in more than one tone and the legend is a gradient rather than a solid bar; the value and the chart's axis read in years; the history chart draws no dashed reference line and no stray line on the baseline; the legend shows no inflection caption; a cell Eurostat flagged reads as such in the detail panel; and the source row names Eurostat with its attribution and licence link. Hard-reload before trusting computed styles. This is the visual confirmation Phase A could not have, and it is the reason the phases are stacked rather than independent.

### Phase B verification

- [ ] T038 Full sweep before handoff: `cargo test -p shared`, `cargo test -p ingestion`, `cargo test -p web --lib`, `cargo check -p web --lib --no-default-features --features hydrate --target wasm32-unknown-unknown`, `cargo check -p web --no-default-features --features ssr`. Expected: clean. The whole ingestion suite matters here, not just the new test binary: `artifact_integration` and `publish_integration` exercise the shard writer that now has to round-trip a status it has never seen.
- [ ] T039 Self-review pass against the root `CLAUDE.md`, `docs/conventions/`, and this feature's own rules before opening the PR. Specifically: no em dashes in comments or the PR description; log and error messages in the `prose; [key=value ...]` shape with no function-name prefix; explicit type annotations on `let` bindings; functions and constants imported through their parent module and types imported directly; no doc comment restating a signature or narrating a field list; the alias and exclusion tables commented with the external fact rather than the mechanism; CLI help text matching the parser's accepted values. Fix what you find before claiming the work is ready.
- [ ] T039a Rewrite `docs/task-order.md` item 1 down to what remains unlanded, the subnational region model and the geometry work, rather than deleting it: Phase B delivers country-level values only, so the item's stated substance survives this feature.
- [ ] T039b `gh pr create --base 010-eurostat-ingestion --assignee zacharysiegel` with the body from plan.md §PR description, Phase B. A stacked branch's PR targets its parent branch, not master, or the diff shows Phase A's commits as well.

---

## Phase C: subnational regions with NUTS-2 values

Sketched in [plan.md](plan.md) §Phase C. No task breakdown: its first task is a probe of Eurostat's regional datasets, and the phase cannot be ordered until the probe settles which datasets carry which indicators at which level, whether they carry the same flag attribute, and how an extraction of them sits against the 500,000-cell synchronous limit. The region-model change, the level-selection question in the client, and a NUTS region lookup are the known substance.

## Phase D: subnational geometry

Sketched in [plan.md](plan.md) §Phase D. No task breakdown, and it cannot have one until a candidate boundary file is downloaded: the phase is one layer if the finest optional layer covers every country ingested, and either a mixed-resolution layer or a two-source join if it does not. The repository's only committed subnational geometry decision names a Natural Earth provinces layer, which does not nest with NUTS for several member states and so cannot carry NUTS values; revisiting that decision is part of the phase.

---

## Deviations from the plan

Recorded here as the work lands.
